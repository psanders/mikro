{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "updater",
  "description": "Auto-update permissions — desktop only. The updater/process plugins are not registered on mobile, so these permissions are scoped away from Android/iOS. Includes dialog:allow-ask because updater.ts prompts with ask() before installing — dialog:default does not cover ask, only message/save/open.",
  "platforms": ["macOS", "windows", "linux"],
  "windows": ["main"],
  "permissions": ["updater:default", "process:allow-restart", "dialog:allow-ask"]
}
