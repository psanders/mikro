# Ship checkpoint — move-loan-statement-to-copilot

Started: 2026-07-10
Current stage: 4 — Test

**Scope:** Retire the `loan-statement` task automation (it was wrongly modeled as schedulable) and move loan-statement generation to an on-demand founder-copilot direct tool (`generateLoanStatement`), reusing the existing `createGenerateLoanStatement` report definition so CLI/tRPC/copilot stay equivalent. Adds a new dock thread-item kind for delivering the document, and tool-disambiguation notes so the model prefers this over `getLoanHealth`/`getLoanByLoanId` for document requests.

**Detected surfaces:** OpenSpec: yes · Pencil: yes (repo uses `pencil.pen`, but this change adds no new screen — see stage 1 skip reason) · Storybook: yes (mods/dashboard) · E2E: no (no playwright.config, no e2e/ dir)

| #   | Stage           | Status      | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| :-- | :-------------- | :---------- | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0   | Frame           | done        | No existing change; scaffolded via openspec-propose. Proposal/design/specs/tasks all written and `openspec validate` passes.                                                                                                                                                                                                                                                                                                                                                       |
| 1   | Design (Pencil) | done        | New screen `QgGhA` ("Founder / Copiloto — estado de cuenta") built by copying `z2TcOH`, thread swapped to a new `documentCard` (`kYC0K`) modeled on `ruleCard`'s color-band-header convention (blue header, file icon, "Documento listo" title, filename subtitle, `cp/btn` download action). No layout problems (`snapshot_layout`). User reviewed and confirmed: "Designs are good."                                                                                             |
| 2   | Spec reconcile  | done        | No drift from the approved design — tokens/copy match what was already speced (DIRECT tool, `document` field/thread-item kind). `openspec validate --strict` passes, `task-automation-catalog` spec confirmed to need no change.                                                                                                                                                                                                                                                   |
| 3   | Build           | done        | Backend + frontend both done. `DocumentCard.tsx` reconciled 1:1 against the approved Pencil node `kYC0K` (colors/spacing/copy match; one drift fixed — download icon 14px → 16px). Stale doc comments referencing "the `loan-statement` automation" as current tightened across `createGenerateLoanStatement.ts`, `protected.ts`, `tasks/types.ts`, `tasks/firings.ts`.                                                                                                            |
| 4   | Test            | in-progress | apiserver: typecheck clean, unit 423 passing, integration 650 passing (incl. new 4-test `generateLoanStatement` describe block). common: typecheck clean, 103 passing. dashboard: typecheck clean, no test script exists (confirmed pre-existing gap, not introduced here). Lint (`apiserver`+`common`+`dashboard` together) running in background — long-running, awaiting result. Task 6.2 (manual copilot verify in the running app) still open — needs a live app to exercise. |
| 5   | Sync            | pending     |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 6   | Archive         | pending     |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |

Status values: `pending` · `in-progress` · `done` · `skipped` (with reason).

## Decision log

Newest first. One line per meaningful decision or stage transition.

- 2026-07-10 — Design (Pencil) stage skipped: no new screens, small addition to existing dock component. Will revisit if build reveals the document card needs real visual design.
- 2026-07-10 — Frame complete: scaffolded change via `openspec new change` + openspec-propose skill (proposal, design, specs for `founder-copilot` [ADDED] and `loan-statement-report` [MODIFIED/REMOVED], tasks). `openspec validate move-loan-statement-to-copilot --strict` passes.
- 2026-07-10 — Design decisions locked: `generateLoanStatement` is a DIRECT tool (read-only, no confirm gate); delivery requires a new `document` field on `copilotChatReplySchema` + new `CopilotMessage` kind (confirmed via research agent: no existing "download this file" pattern in the dock — the loan-form contract download and the task-firing attachment both bypass the chat channel entirely); tool-disambiguation note added to system prompt to fix the observed misrouting to `getLoanHealth`.
- 2026-07-10 — User confirmed the automation modeling was wrong from the start ("Estado de cuenta as an automation makes no sense at all... always meant to be something we could request via the Copilot") — scope is retire automation + move to copilot, not just add a new invocation path alongside the automation.
