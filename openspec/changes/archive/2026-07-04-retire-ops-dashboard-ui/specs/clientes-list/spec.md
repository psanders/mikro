# clientes-list — delta

## REMOVED Requirements

### Requirement: Customers list screen

**Reason**: The operations dashboard UI is retired (founder direction confirmed 2026-07-04); only UI requirements are removed — the underlying APIs and capabilities stay.
**Migration**: Clients are reachable via founder search and the copilot; `listCustomers` is unchanged.

### Requirement: Active/inactive segmentation

**Reason**: The operations dashboard UI is retired (founder direction confirmed 2026-07-04); only UI requirements are removed — the underlying APIs and capabilities stay.
**Migration**: Clients are reachable via founder search and the copilot; `listCustomers` is unchanged.

### Requirement: Server-side search

**Reason**: The operations dashboard UI is retired (founder direction confirmed 2026-07-04); only UI requirements are removed — the underlying APIs and capabilities stay.
**Migration**: Clients are reachable via founder search and the copilot; `listCustomers` is unchanged.

### Requirement: Pagination

**Reason**: The operations dashboard UI is retired (founder direction confirmed 2026-07-04); only UI requirements are removed — the underlying APIs and capabilities stay.
**Migration**: Clients are reachable via founder search and the copilot; `listCustomers` is unchanged.
