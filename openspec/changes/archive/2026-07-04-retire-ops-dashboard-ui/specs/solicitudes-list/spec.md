# solicitudes-list — delta

## REMOVED Requirements

### Requirement: Applications list screen

**Reason**: The operations dashboard UI is retired (founder direction confirmed 2026-07-04); only UI requirements are removed — the underlying APIs and capabilities stay.
**Migration**: Applications are reachable via founder search, the feed, the copilot, and the mobile evaluator app; `listApplications` is unchanged.

### Requirement: Status filtering and search

**Reason**: The operations dashboard UI is retired (founder direction confirmed 2026-07-04); only UI requirements are removed — the underlying APIs and capabilities stay.
**Migration**: Applications are reachable via founder search, the feed, the copilot, and the mobile evaluator app; `listApplications` is unchanged.

### Requirement: Pagination

**Reason**: The operations dashboard UI is retired (founder direction confirmed 2026-07-04); only UI requirements are removed — the underlying APIs and capabilities stay.
**Migration**: Applications are reachable via founder search, the feed, the copilot, and the mobile evaluator app; `listApplications` is unchanged.

### Requirement: Access is reviewer-gated in the UI

**Reason**: The operations dashboard UI is retired (founder direction confirmed 2026-07-04); only UI requirements are removed — the underlying APIs and capabilities stay.
**Migration**: Applications are reachable via founder search, the feed, the copilot, and the mobile evaluator app; `listApplications` is unchanged.
