# modelo-report — delta

## REMOVED Requirements

### Requirement: Export the projection model as a branded PDF

**Reason**: The operations dashboard UI is retired (founder direction confirmed 2026-07-04); only UI requirements are removed — the underlying APIs and capabilities stay.
**Migration**: The projection engine remains in `@mikro/common/projection`; the branded PDF export retires with the desktop UI (re-exposable later as a founder report).

### Requirement: Saving works in both Tauri and web

**Reason**: The operations dashboard UI is retired (founder direction confirmed 2026-07-04); only UI requirements are removed — the underlying APIs and capabilities stay.
**Migration**: The projection engine remains in `@mikro/common/projection`; the branded PDF export retires with the desktop UI (re-exposable later as a founder report).

### Requirement: Projection is computed from a single shared engine

**Reason**: The operations dashboard UI is retired (founder direction confirmed 2026-07-04); only UI requirements are removed — the underlying APIs and capabilities stay.
**Migration**: The projection engine remains in `@mikro/common/projection`; the branded PDF export retires with the desktop UI (re-exposable later as a founder report).
