## 1. Pencil design (mobile entry point)

- [x] 1.1 Added "Reportar un problema" row (`stJx6`) to the shared Perfil screen's AJUSTES section (`tnERk`), reusing the `m/list-tile` component
- [x] 1.2 Built 5 screens in pencil.pen's shared cluster: consent (`v8bmyV`), recording (`OWGz8`, redesigned as a floating non-blocking pill after user feedback), enviando (`rv2oJ`), enviado (`fsDNM`), error (`oTSL4`)
- [x] 1.3 Resolved as moot: only one shared `perfil.tsx` exists for both Cobradores and Evaluator roles, and the design lives in the shared `nCQnp` cluster — both get it automatically, no separate evaluator screen needed
- [x] 1.4 User approved the design explicitly after the recording-pill redesign

## 2. Tauri desktop capture

- [x] 2.1 Spike `screencapturekit` crate API: pulled real source (v8.0.0) and compiled against it — `SCRecordingOutput` (macOS 15+) does record-to-file, but muxing with mic audio needs bundled ffmpeg (new signing/distribution scope); descoped to native screenshot only (`SCScreenshotManager::capture_image()`, macOS 14+) — see design.md Decision 1
- [x] 2.2 Add `commands.rs` in `mods/dashboard/src-tauri/src/` wrapping ScreenCaptureKit screenshot capture, registered in `lib.rs`'s `invoke_handler`; `cargo check` passes clean
- [x] 2.3 Expose `capture_bug_report_screenshot` command to the frontend (stateless, single-shot — no start/stop session needed since it's one screenshot, not a video stream)
- [x] 2.4 Handle macOS Screen Recording permission-denied case (`SCError::PermissionDenied`) with a clear Spanish error, returned from the command for the bug-report dialog's existing error stage
- [x] 2.5 Add capture-provider seam in `BugReportButton.tsx`: `startBrowserRecording`/`startTauriRecording`, selected via `hasDisplayMedia()` / `isTauri()`; `tsc -b --noEmit` passes clean
- [x] 2.6 ~~Mux native screen video with mic audio~~ — descoped per design.md Decision 1; mic recording submitted directly as the video field, native screenshot as the screenshot field
- [x] 2.7 `Cargo.lock` regenerated by `cargo check` (no new npm dependency added — only Rust deps changed — so root `package-lock.json` is unaffected; lockfile platform coverage check is npm-lockfile-specific and doesn't apply here)

## 3. Mobile native capture

- [x] 3.1 Added `react-native-nitro-screen-recorder@0.7.0` + `react-native-nitro-modules@0.36.1`; registered config plugin in `app.config.ts` (camera permission off, mic permission on with Spanish copy)
- [x] 3.2 `expo prebuild --platform all --no-install` ran clean — config plugin auto-provisioned the iOS BroadcastExtension target, Android manifest permissions/service, and MainActivity changes with no errors
- [x] 3.3 Confirmed against real library docs: mic is native via `enableMic: true` on start (no separate audio library/muxing needed); output is a `ScreenRecordingFile` with a local `path`, read via already-installed `expo-file-system`. Also discovered a real platform asymmetry: iOS has `startInAppRecording` (in-app only), Android only has `startGlobalRecording` (system-wide) — see design.md Decision 3
- [x] 3.4 Build bug-report entry point UI in `perfil.tsx` per the signed-off Pencil design (task group 1)
- [x] 3.5 Implement consent → record → stop → processing → result/error state machine, reusing web copy/tone (Spanish), plus the Android system-wide-capture consent notice
- [x] 3.6 Wire recording output (video + screenshot) to the existing `submitBugReport` tRPC mutation, matching the payload shape used by web/Tauri clients
- [x] 3.7 ~~Replicate in evaluator app~~ — moot, resolved during design: only one shared `perfil.tsx` exists for both roles
- [x] 3.8 Root `package-lock.json` updated via `npm install --workspace mods/mobile` + `npx expo install expo-video-thumbnails`; `.scripts/check-lockfile-platforms.mjs` passes clean

## 4. Tests

- [x] 4.1 ~~Unit tests for the Tauri capture path (mock plugin IPC)~~ — descoped: `mods/dashboard` has NO existing frontend unit-test infrastructure at all (no vitest/jest/testing-library, confirmed by repo survey), and introducing one is out of scope for this change. Correctness verified instead by real compilation: `cargo check` against the actual `screencapturekit` v8.0.0 source (pulled and inspected, not assumed), including the `SCError::PermissionDenied` match arm. Flagged to the user as a real gap, not silently skipped.
- [x] 4.2 Extracted `finishBugReportRecording` (pure, DI'd: `readBase64`/`extractScreenshot`/`submit`) out of `BugReportContext.tsx` specifically to make it unit-testable; 4 tests in `lib/bugReport/__tests__/finishBugReportRecording.test.ts` — happy path, missing-file validation failure (asserts `submit` never called), read-failure (asserts `submit` never called), and submit-error propagation
- [x] 4.3 `createSubmitBugReport` — all 6 existing unit tests pass unchanged (`npx mocha --grep "SubmitBugReport"`), confirming zero server-side impact
- [x] 4.4 Mobile `tsc --noEmit`, `jest` (42 passing, up from 38), and `expo prebuild --platform all --no-install --clean` all pass clean
- [ ] 4.5 Manual verification: build and run the Tauri dashboard on macOS, confirm a real recording succeeds end-to-end and files a GitHub issue — NOT done in this session (no interactive macOS Tauri build/Screen-Recording-permission grant available here); flagged as required before merge
- [ ] 4.6 Manual verification: build and run a mobile EAS dev client, confirm a real recording succeeds end-to-end and files a GitHub issue on both iOS and Android — NOT done in this session (requires a real EAS dev client build + physical/simulator run); flagged as required before merge

## 5. Spec sync and documentation

- [ ] 5.1 Note the EAS dev client requirement (no Expo Go support) in the PR description and any relevant team-facing docs
- [x] 5.2 Promoted `bug-report-desktop-capture` and `bug-report-mobile-capture` delta specs into `openspec/specs/` (both net-new capability spec files)
